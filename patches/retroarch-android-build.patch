diff --git a/pkg/android/phoenix/build.gradle b/pkg/android/phoenix/build.gradle
index 03168ab..a3d5542 100644
--- a/pkg/android/phoenix/build.gradle
+++ b/pkg/android/phoenix/build.gradle
@@ -1,6 +1,7 @@
 buildscript {
   repositories {
     google()
+    mavenCentral()
     jcenter()
   }
 
@@ -12,6 +13,7 @@ buildscript {
 allprojects {
     repositories {
         google()
+        mavenCentral()
         jcenter()
     }
 
@@ -51,13 +53,18 @@ android {
       dimension "variant"
     }
     aarch64 {
-      applicationIdSuffix '.aarch64'
-      resValue "string", "app_name", "RetroArch (AArch64)"
+      // Richard: applicationId UNIQUE pour s'installer A COTE de RetroArch
+      // officiel (com.retroarch.aarch64) sans proposer une mise a jour.
+      // NB: on garde le package de code com.retroarch (manifest) -> JNI natif intact.
+      // Parametrable (-PAPP_ID / -PAPP_NAME) pour produire des variantes (ex 3:2).
+      applicationId project.hasProperty('APP_ID')   ? APP_ID   : "com.sfa3.widescreen"
+      resValue "string", "app_name", project.hasProperty('APP_NAME') ? APP_NAME : "SFA3 Widescreen"
       buildConfigField "boolean", "PLAY_STORE_BUILD", "false"
 
       dimension "variant"
       ndk {
-        abiFilters 'arm64-v8a', 'x86_64'
+        // Richard: arm64 uniquement (notre core widescreen est arm64-v8a).
+        abiFilters 'arm64-v8a'
       }
     }
     ra32 {
